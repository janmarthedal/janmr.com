---
layout: blog-page
---
<h2>Posts tagged <em>{{ data.tag }}</em></h2>

<div class="post-list">
{% for post in data.posts | reverse -%}
  <h5>
    <a href="{{ post.url | url }}">{{ post.title }}</a> <small>{{ post.date | readableDate }}</small>
  </h5>
{%- endfor %}
</div>
